#!/bin/bash

while getopts i: option
  do
    case "${option}"
      in
      i) KEY=${OPTARG}
      shift
      shift
      ;;
    esac
  done

ID=`ssh -i $KEY $1 "docker ps -qf 'ancestor=gversmee/openvpn-server'"`
ssh -i $KEY $1 "docker stop $ID ; sudo rm -rf ~/client_config"
